/** Shared constants for the aliases reference app. */
export const SCROLL_THRESHOLD = 60;
export const NAV_HIDE_DELAY = 2500;
export const TYPING_SPEED = 45;
export const OUTPUT_DELAY = 400;
export const RESTART_DELAY = 3000;
export const PASSIVE = { passive: true };

/** CSS selector map. */
export const S = Object.freeze({
  mainNav: "#mainNav",
  aliasSearch: "#aliasSearch",
  themeBtn: "#themeBtn",
  backToTop: "#backToTop",
  aliasCard: ".alias-card",
  tocLinks: "#tocSidebar .nav-link",
  sidebarClose: "#sidebarCloseBtn",
  sidebarToggle: "#sidebarToggleBtn",
  sidebar: "#tocSidebar",
  navHoverZone: "#navHoverZone",
  demoBtn: ".demo-btn",
});
